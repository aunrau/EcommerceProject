<h1>Product Listing</h1>

<p><%= will_paginate @products, renderer: BootstrapPagination::Rails %></p>

<%= render @products %>

<%= link_to 'Show', @product %>
<%# <%= link_to 'New Product', new_product_path %>
